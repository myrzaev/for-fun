import { claimbingStairCase } from './index';

describe('Cartesian Product', () => {
	it('Cartesian Product', () => {
		expect(claimbingStairCase(1)).toEqual(1);
		expect(claimbingStairCase(2)).toEqual(2);
		expect(claimbingStairCase(3)).toEqual(3);
		expect(claimbingStairCase(4)).toEqual(5);
	});
});
